# ~/.config/hypr/modules/keybinds.conf -- keybindings and shortcuts
# see https://wiki.hypr.land/Configuring/Keywords/
# see https://wiki.hypr.land/Configuring/Binds/ for more

$mainMod = SUPER # sets "windows/super" key as main modifier

# application launchers
bind = $mainMod, Return, exec, $terminal # terminal emulator
bind = $mainMod, E, exec, $fileManager # file manager
bind = $mainMod, B, exec, /usr/bin/helium-browser # web browser
bind = ALT, Space, exec, vicinae toggle # raycast clone
bind = $mainMod, space, exec, rofi -show drun || pkill rofi # rofi

# window management
bind = $mainMod, Q, killactive, # kill active window
bind = $mainMod, V, togglefloating, # toggle floating, pinch in on trackpad
bind = $mainMod, P, pseudo, # centered pseudo mode ( dwindle )
bind = $mainMod, J, togglesplit, # switches b/w horizontal and vertical ( dwindle )

# layout
bind = $mainMod, bracketleft, exec, hyprctl keyword general:layout dwindle # super + [ - dwindle layout
bind = $mainMod, bracketright, exec, hyprctl keyword general:layout scrolling # super + ] - scrolling layout, see ./plugins/hyprscrolling.conf for info

# focus movement
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# workspaces
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# move window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# workspace scrolling
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# move and resize windows
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# waybar
bind = $mainMod, R, exec, ~/.config/waybar/scripts/launch.sh
bind = $mainMod, W, exec, killall -SIGUSR1 waybar

# system
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit

# audio
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# brightness
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# screenshots
bind = , Print, exec, grimblast copysave screen ~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png # ( full screen ) save in folder & copy to clip
bind = CTRL, Print, exec, grimblast copysave area ~/Pictures/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png # ( selection mode ) save in folder & copy to clip

# hyprscrolling (disabled)
# bind = $mainMod, period, layoutmsg, move +col
# bind = $mainMod, comma, layoutmsg, move -col
# bind = $mainMod SHIFT, period, layoutmsg, movewindowto r
# bind = $mainMod SHIFT, comma, layoutmsg, movewindowto l
# bind = $mainMod SHIFT, up, layoutmsg, movewindowto u
# bind = $mainMod SHIFT, down, layoutmsg, movewindowto d

